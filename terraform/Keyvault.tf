# No Key Vault for this deployment.
# The OpenAI API key is passed as a secure environment variable directly into
# the ACI container (visible only at apply time, not in the Azure portal UI).
# This is sufficient for a short-lived research deployment.
